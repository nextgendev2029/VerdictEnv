# Minimal server install for Docker / Space builds that `pip install -r server/requirements.txt`
openenv-core[core]>=0.2.2
fastapi>=0.115.0
pydantic>=2.0.0
uvicorn[standard]>=0.24.0
websockets>=12.0
httpx>=0.27.0
gradio>=4.44.0
